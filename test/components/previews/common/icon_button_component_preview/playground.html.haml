= render(BaseComponent.new(tag: :div, class: "inline-flex items-center justify-center gap-2")) do
  = render(Common::IconButtonComponent.new(icon:, size:, scheme:))
  = render(Common::IconButtonComponent.new(icon:, size:, scheme:))
  = render(Common::IconButtonComponent.new(icon:, size:, scheme:, disabled: true))
  = render(Common::IconButtonComponent.new(icon:, size:, scheme:))
  = render(Common::IconButtonComponent.new(icon:, size:, scheme:, autofocus: true))
