= render BaseComponent.new(**content_tag_arguments) do
  -# Sidebar
  .h-full.w-64
    = render Sidebar.new

  -# Content
  %turbo-frame.flex-1.h-full.flex.flex-col.gap-6.px-20.py-12.overflow-hidden{ id: BODY_FRAME_ID }
    = content
