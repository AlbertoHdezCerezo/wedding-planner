= render BaseComponent.new(**content_tag_args) do
  %li
    - if heading?
      = heading
    - if items?
      - items_wrapper_arguments = { tag: :ul, "aria-labelledby": rendered_heading.id }.compact
      = render BaseComponent.new(**items_wrapper_arguments) do
        - items.each do |item|
          = item
