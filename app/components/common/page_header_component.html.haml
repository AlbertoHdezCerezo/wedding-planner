= render BaseComponent.new(**content_tag_args) do
  -# Header
  .px-12.pt-8.flex.flex-row.items-center.justify-between
    -# Title
    .flex.flex-col.items-start.justify-between.gap-3
      -# Context Bar
      - if back_url.present? && back_label.present?
        = link_to(back_url, class: "flex flex-row items-center gap-2 mb-2 text-gray-500 hover:text-tiffany_blue-500") do
          = icon_component(icon: "arrow-left", size: :xs)
          %p.text-xs= back_label

      = title
      = description

  - if navigation.present?
    = navigation
